auto lo
iface lo inet loopback

rename enp2s0=eth1
auto eth1
  iface eth1 inet static
  address 10.0.2.1
  netmask 255.255.255.0"

rename wlp1s0=eth0
auto eth0
  iface eth0 inet dhcp
  wpa-ssid {NETWORK_NAME}
  wpa-psk {NETWORK_PASSWD}
